<div class="auth-wrapper">
    <div class="auth-card glass-card animate-fade-in">
        <div class="auth-header" id="authHeader">
            <div class="auth-logo">
                <i class="fa-solid fa-key"></i>
            </div>
            <h1>Password Recovery</h1>
            <p>Enter your institutional email to receive reset instructions</p>
        </div>
        
        <div id="alertBox"></div>

        <form id="forgotForm" class="auth-form">
            <div class="form-group mb-4">
                <label><i class="fa-solid fa-envelope"></i> Administrator Email</label>
                <input type="email" id="email" required placeholder="name@university.ac.uk" class="glass-input">
            </div>
            
            <button type="submit" class="btn-primary auth-btn" id="resetBtn">
                <span>Send Reset Link</span>
                <i class="fa-solid fa-paper-plane"></i>
            </button>
        </form>

        <div class="auth-footer">
            <p>Secure Access Protocol</p>
            <div class="auth-links">
                Remember your password? <a href="/login">Return to Login</a>
            </div>
        </div>
    </div>
</div>

<script>
document.getElementById('forgotForm').addEventListener('submit', async (e) => {
    e.preventDefault();
    const email = document.getElementById('email').value;
    const btn = document.getElementById('resetBtn');
    const alertBox = document.getElementById('alertBox');
    const header = document.getElementById('authHeader');

    btn.disabled = true;
    btn.innerHTML = '<span>Processing...</span> <i class="fa-solid fa-circle-notch fa-spin"></i>';
    alertBox.innerHTML = '';

    try {
        const response = await fetch('/api/auth/forgotpassword', {
            method: 'POST',
            headers: { 'Content-Type': 'application/json' },
            body: JSON.stringify({ email })
        });
        const data = await response.json();
        
        // Compact spacing when alert is shown
        header.style.marginBottom = "0.75rem";

        if (data.success) {
            alertBox.innerHTML = '<div class="alert alert-success" style="margin-bottom: 0.75rem; padding: 0.6rem 1rem;"><i class="fa-solid fa-circle-check"></i> Reset instructions sent! Please check your email.</div>';
            document.getElementById('email').value = '';
            
            // Re-enable button after showing success
            setTimeout(() => {
                btn.disabled = false;
                btn.innerHTML = '<span>Send Reset Link</span> <i class="fa-solid fa-paper-plane"></i>';
            }, 3000);
        } else {
            alertBox.innerHTML = `<div class="alert alert-error" style="margin-bottom: 0.75rem; padding: 0.6rem 1rem;"><i class="fa-solid fa-circle-exclamation"></i> ${data.message || 'Request failed'}</div>`;
            btn.disabled = false;
            btn.innerHTML = '<span>Send Reset Link</span> <i class="fa-solid fa-paper-plane"></i>';
        }
    } catch (err) {
        alertBox.innerHTML = '<div class="alert alert-error" style="margin-bottom: 0.75rem; padding: 0.6rem 1rem;"><i class="fa-solid fa-triangle-exclamation"></i> Connectivity error.</div>';
        btn.disabled = false;
        btn.innerHTML = '<span>Send Reset Link</span> <i class="fa-solid fa-paper-plane"></i>';
    }
});
</script>

<style>
.auth-wrapper {
    min-height: 100vh;
    display: flex;
    align-items: center;
    justify-content: center;
    padding: 2rem;
    background: radial-gradient(circle at top right, rgba(99, 102, 241, 0.1), transparent),
                radial-gradient(circle at bottom left, rgba(6, 182, 212, 0.1), transparent);
}

.auth-card {
    width: 100%;
    max-width: 420px;
    padding: 1.5rem 2rem !important;
    text-align: center;
    border: 1px solid rgba(255, 255, 255, 0.1);
}

.auth-header { margin-bottom: 1.25rem; transition: all 0.3s ease; }

.auth-logo {
    width: 44px;
    height: 44px;
    background: linear-gradient(135deg, var(--primary), var(--secondary));
    border-radius: 10px;
    display: flex;
    align-items: center;
    justify-content: center;
    margin: 0 auto 1rem;
    font-size: 1.25rem;
    color: #fff;
    box-shadow: 0 10px 20px rgba(99, 102, 241, 0.3);
}

.auth-header h1 {
    font-size: 1.5rem;
    font-weight: 800;
    margin-bottom: 0.15rem;
    letter-spacing: -0.5px;
}

.auth-header p {
    color: var(--text-muted);
    font-size: 0.85rem;
}

.glass-input {
    background: rgba(15, 23, 42, 0.4) !important;
    border: 1px solid var(--glass-border) !important;
    color: #fff !important;
    padding: 0.75rem 1rem !important;
    border-radius: 8px !important;
    width: 100%;
    transition: all 0.3s ease !important;
}

.glass-input:focus {
    border-color: var(--primary) !important;
    box-shadow: 0 0 0 4px rgba(99, 102, 241, 0.15) !important;
    background: rgba(15, 23, 42, 0.6) !important;
}

.auth-btn {
    width: 100%;
    padding: 0.8rem !important;
    font-size: 0.95rem !important;
    margin-top: 0.25rem;
    justify-content: center;
    display: flex;
    align-items: center;
    gap: 10px;
}

.auth-footer {
    margin-top: 1.5rem;
    padding-top: 1rem;
    border-top: 1px solid var(--glass-border);
}

.auth-footer p {
    font-size: 0.7rem;
    text-transform: uppercase;
    letter-spacing: 2px;
    color: var(--text-muted);
    margin-bottom: 0.5rem;
}

.auth-links {
    font-size: 0.95rem;
    color: var(--text-muted);
}

.auth-links a {
    color: var(--primary-light);
    text-decoration: none;
    font-weight: 600;
    margin-left: 5px;
}

.auth-links a:hover {
    text-decoration: underline;
}

.alert {
    border-radius: 12px;
    font-size: 0.9rem;
    display: flex;
    align-items: center;
    gap: 10px;
    text-align: left;
}

.alert-error {
    background: rgba(244, 63, 94, 0.1);
    border: 1px solid rgba(244, 63, 94, 0.2);
    color: #f43f5e;
}

.alert-success {
    background: rgba(16, 185, 129, 0.1);
    border: 1px solid rgba(16, 185, 129, 0.2);
    color: #10b981;
}

.animate-fade-in { animation: fadeIn 0.5s ease-out; }
@keyframes fadeIn { from { opacity: 0; transform: translateY(10px); } to { opacity: 1; transform: translateY(0); } }
</style>
